spring:
  datasource:
    url: jdbc:mysql://mariadb:3306/test_auto
    username: skala_user
    password: skalapass
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    mongodb:
      uri: mongodb://mongodb:27017/test_auto

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect
